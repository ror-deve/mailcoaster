<%# locals: (resource: []) -%>
<% if resource.present? %>
  <div class="alert alert-danger">
    <a class="close" data-bs-dismiss="alert">×</a>
    <% resource.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </div>
<% end %>
